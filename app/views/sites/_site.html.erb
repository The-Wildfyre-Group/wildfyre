
<%= link_to site.name, site %> |

<%= site.status %> |

<%= site.url %> |

<%= site.renewal_date %>

<%= link_to "Delete", site, method: :delete, data: {confirm: "Are you sure?"}, remote: true %> |

<%= link_to "Edit", edit_site_path(site) %>
<br><br>
